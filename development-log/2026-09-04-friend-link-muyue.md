# 2026-09-04 新增木月友链

## 本次调整

- 新增木月的 Bilibili 友链，包含公开主页名称、头像与签名。
- 使用 `muyue` 作为友链 `id`，继续由数据层按 `id` 英文字母顺序自动排列。
- 在友链页面的介绍侧栏中增加修改、增删友链的联系说明。

## 校验

- `pnpm validate`
- 木月远程头像返回 HTTP 200 与 `image/jpeg`。
- `git diff --check`

