---
interface Props {
  src: string;
  alt: string;
  class?: string;
  sizes?: string;
  position?: string;
  eager?: boolean;
  width?: number;
  height?: number;
}

const {
  src,
  alt,
  class: className,
  sizes = '(max-width: 760px) 100vw, 50vw',
  position = 'center',
  eager = false,
  width,
  height,
} = Astro.props;
---

<picture class={className}>
  <source
    type="image/webp"
    srcset={`${src}-sm.webp 720w, ${src}-lg.webp 1920w`}
    {sizes}
  />
  <img
    src={`${src}-lg.webp`}
    {alt}
    loading={eager ? 'eager' : 'lazy'}
    fetchpriority={eager ? 'high' : 'auto'}
    decoding="async"
    {width}
    {height}
    style={`object-position: ${position}`}
  />
</picture>
